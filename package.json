{
  "name": "happypawsapp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start-local": "concurrently \"npm run dev --prefix frontend\" \"npm run start --prefix backend\"",
    "dev": "echo \"Please run start-local to start both the frontend and backend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
